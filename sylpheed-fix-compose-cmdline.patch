--- src/main.c.orig	Thu May 17 09:59:09 2001
+++ src/main.c	Thu May 17 10:26:26 2001
@@ -445,6 +445,10 @@
 static void open_compose_new_with_recipient(const gchar *address)
 {
 	gchar *addr = NULL;
+	gchar *s;
+
+	/* strip trailing new line in compose command */
+	for (s = address; *s; s++) if (*s == '\n') *s = '\0';
 
 	if (address) {
 		Xstrdup_a(addr, address, return);
